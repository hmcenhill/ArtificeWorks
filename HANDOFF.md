# Handoff — current state

> **Protocol (for Claude):** This is the living hub between conversations. Before the conversation ends, if anything changed: update *Current state*, *Next up*, and *Open decisions*; add a one-line entry to the *Log*; prune anything no longer true. Keep this file under ~80 lines — when a decision becomes permanent, move it to `docs/` (architecture.md or the relevant epic) and drop it from here. Commit this file with the work it describes.

**Last updated:** 2026-07-21 (Epic 5 complete)

## Current state

- Project renamed from EventDrivenOrderProcessingSystem to **ArtificeWorks** (commit `21b1753`): solution, projects, namespaces, `ArtificeWorksDbContext`, DB name, docker containers, CI.
- Plan fully rewritten for the manufacturing pivot (commit `d218f43`): 17 epics / 7 milestones in `docs/Plan/`. **Epics 1–3 complete** — synchronous core done: work-order domain + state machine, catalog/work-order REST API, RFC 7807 ProblemDetails contract, full unit + integration coverage. Per-story detail in `docs/Plan/` and the Log below.
- **Epic 4 (messaging) complete** — the async backbone: typed event contracts + RabbitMQ publisher (4.1), worker consumption + dispatch (4.2), correlation into log scopes on both services (4.3). Direct exchange `artifice.events`, queue `artifice.workers`, prefetch 1, manual acks, nack-without-requeue, best-effort at-most-once publish — deliberately simple; retry/outbox/DLQ arrive in Epic 8. **The layout and rationale now live in [docs/messaging-topology.md](docs/messaging-topology.md)** — read that rather than re-deriving it. Adding a consumer is still one `AddEventHandler<TEvent,THandler>()` call.
- **Epic 5 (material picking) complete** — the first real workflow stage, done in one pass (5.1–5.4). A scheduled work order now visibly consumes inventory: `WorkOrderScheduled` → worker → BOM expanded → components reserved all-or-nothing → `MaterialsReserved` published back onto the bus for Epic 6. **90 tests green (57 unit + 33 integration).** Detail below.
  - **5.1 model + seed.** New domain: `Component` (fungible input material with an `OnHand` count — distinct from `Product`, the finished automaton, and from `StockKeepingUnit`, a serialized finished unit), `BomLine` (flat BOM entry on `Product`), `ComponentDemand`, `MaterialReservation`/`MaterialReservationLine`. `Product.ComputeDemand(qty)` expands the BOM (`QtyPerUnit × OrderItemQty`) **ordered by component id** — that ordering is load-bearing for deadlock avoidance, not cosmetic. Migration `BomAndComponentInventory` (4 tables, unique index on `material_reservations.WorkOrderId`, unique `(ProductId, component_id)` on BOM lines, `CHECK (on_hand >= 0)`). `CatalogSeeder` (idempotent, additive, runs at API startup when no migrations are pending) seeds 7 shared platform components + 3 trade-specific per line — **every product is exactly 70% shared, asserted in a test, not just claimed**. `GET /products/{id}` now returns the BOM with per-component on-hand.
  - **5.2 picking consumer.** `Application/Materials/MaterialPickingService` owns the workflow (loads order → dedupe pre-check → expand BOM → reserve → note history → publish); the worker's `WorkOrderScheduledHandler` is now a thin adapter that adopts the inbound correlation id and logs the outcome. The 4.2 "scheduling acknowledged" stand-in is gone. New event `MaterialsReserved` (`work-order.materials-reserved`). **Picking deliberately does not advance Scheduled → InProcess** — that's Epic 6's transition; picking hands over by publishing. Workers now uses `AddRabbitMqMessaging` (it publishes as well as consumes).
  - **5.3 all-or-nothing + concurrency.** `MaterialReservationRepository.TryReserve` runs one transaction: per line, an **atomic conditional decrement** (`UPDATE components SET on_hand = on_hand - @q WHERE "ComponentId" = @id AND on_hand >= @q`, checking rows-affected); zero rows → roll the whole thing back, so a partial BOM leaves on-hand exactly as it was. No aggregate rowversion needed (consistent with the standing no-optimistic-concurrency decision). Shortage → `SetHold` naming the short component, reason in state history, **message acked** (business outcome, not a fault). Proven by a real 10-way race against stock for 4: exactly 4 reserve, 6 go OnHold, `sum(reserved) == on_hand`.
  - **5.4 idempotency.** The reservation row **is** the dedupe key (unique index on `WorkOrderId`), so the marker and the reservation commit atomically by construction — no inbox table. A cheap pre-check handles the common case; the constraint handles the race (loser's `DbUpdateException` 23505 → rollback takes its decrements with it). Duplicates ack, are logged, and deliberately write **no** history note (a note per redelivery would itself be non-idempotent). Both sequential and simultaneous redelivery are tested.
  - **Testing shape worth knowing:** the reservation guarantees are raced in `MaterialPickingTests` (Postgres only, workflow invoked in parallel scopes) because the broker path *can't* prove them — prefetch 1 on a single consumer serializes deliveries. `WorkerConsumerTests` (Postgres + RabbitMQ) owns the plumbing claim end-to-end, including observing `MaterialsReserved` on the bus with the original correlation id.

## Next up

1. **Apply the new migration locally** (`dotnet ef database update ...`, see Notes.md) — then starting the API seeds the shared-platform catalog automatically, and `GET /products/CUSTODIAN-STD` shows the BOM. Worth an eyeball before Epic 6.
2. **Epic 6 — Production and inspection workflow.** Not yet groomed (epic file only). Its first consumer hangs off `work-order.materials-reserved`, which Epic 5 already publishes and nothing yet binds; it also owns the Scheduled → InProcess advance that picking deliberately left alone. **Groom it into stories first**, as with Epics 4 and 5.
3. **Small follow-ups worth folding in when convenient:** map `WorkOrderStateHistory.CompletedBy` + migration (see Open decisions); release-from-hold does not re-trigger picking (see Open decisions); the AMQP `IncludeScopes` appsettings path vs code (documented in the 4.3 log).

## Open decisions

- `SetStatus` (superuser override) still has no endpoint — deferred to admin auth, as planned.
- **No optimistic concurrency on `WorkOrder`** (no rowversion / `xmin`). Concurrent *same-order* advances would duplicate history / lose updates silently — so 3.4 tests concurrent *create* only. Deferred; the Epic 4 async pivot moves transitions onto a queue and will revisit aggregate concurrency. (Detailed in `docs/Plan/.../3.4.md`.)
- **`WorkOrderStateHistory.CompletedBy` is silently unmapped by EF** (discovered in 4.2) — it's absent from the model snapshot, so the "who did this transition" author is never persisted (always null in the DB), even though every transition sets it. Latent since the property was added without a migration. Not fixed in 4.2 (out of scope; needs a mapping + migration); the worker E2E test asserts on `Notes` instead. **Worth a small follow-up:** map `CompletedBy` + add a migration.
- **Publishing is best-effort, at-most-once** (4.1, and now also for `MaterialsReserved`). Publish-after-commit with a swallow-and-log on broker failure: a state change can persist while its event is dropped (the classic dual-write gap). Chosen to keep the synchronous core resilient to broker outages. Epic 8's transactional outbox closes it.
- **Releasing a held work order does not re-trigger picking** (5.3). An order held for insufficient stock stays unpicked until someone releases it via the existing endpoint — and release only restores the previous status, it publishes nothing. Fine while stock never restocks itself; the simulation engine (Epic 10) is the natural place to close the loop.
- **The pick's state-history note is written outside the reservation transaction** (5.2). The decrements + reservation row commit atomically (that's the invariant); the note is a second save. A crash in between leaves a correct reservation with no note, and the redelivery would be a no-op duplicate — so the audit trail can lose a line the inventory never does. Deliberate: keeping audit out of the hot transaction. Revisit if the dashboard ends up depending on the note.

## User to-dos (not Claude's)

- Recreate local DB after rename: `docker compose down -v && docker compose up -d`, then EF update (see Notes.md).
- Rename GitHub repo + local folder to match ArtificeWorks (deferred by choice).
- Push commits when ready.

## Log

- **2026-07-21** — **Epic 5 complete** (5.1–5.4 in one pass): the first real workflow stage. `Component` + flat `BomLine` BOM + `MaterialReservation` domain, migration `BomAndComponentInventory`, idempotent `CatalogSeeder` (70%-shared platform, asserted). `MaterialPickingService` replaces the 4.2 stand-in: expands the BOM, reserves all-or-nothing via an **atomic conditional decrement** in one transaction, holds the order with a named shortage when short, and publishes `MaterialsReserved` for Epic 6. Idempotency = a unique index on `material_reservations.WorkOrderId` (the reservation row *is* the dedupe key). Worker now publishes as well as consumes, carrying the inbound correlation id forward. New `MaterialPickingTests` (races 10 picks against stock for 4), `WorkerConsumerTests` rewritten end-to-end. `docs/messaging-topology.md` updated. 90 tests green (57 unit + 33 integration).
- **2026-07-18** — Groomed Epic 5 into four story files (5.1–5.4) + Stories index in `EPIC_5.md`. Pipeline-first arc: model+seed → happy-path picking → insufficient-stock/concurrency → idempotency. Each maps to the epic's acceptance criteria and carries a proposed-decisions block. New entities proposed: `Component` (on-hand inventory, distinct from `Product`), `BomLine`, `MaterialReservation` (per-order pick record, also the idempotency key).
- **2026-07-18** — Story 4.3: correlation into log scopes + topology doc (Epic 4 complete). Shared `CorrelationLog.BeginCorrelationScope` (message-template scope → readable console + structured `CorrelationId` field); middleware opens it per request, consumer per delivery from the AMQP `correlation_id`. `AddSimpleConsole(IncludeScopes)` in both hosts. `docs/messaging-topology.md` + README link. `CorrelationMiddlewareTests`. Verified live: one grep spans both services. 76 tests green.
- **2026-07-18** — Story 4.2: worker consumption + dispatch. Workers → Generic Host consuming `artifice.events`; `IIntegrationEventHandler<T>` + `EventDispatcher` + `RabbitMqConsumerService` (manual acks, nack `requeue:false`); `WorkOrderScheduledHandler` appends a state-history note via new `WorkOrder.AppendNote`. Extracted `AddRabbitMqConnection`. Real publish→consume E2E (`WorkerConsumerTests`, Testcontainers RabbitMQ+Postgres). Flagged unmapped `CompletedBy`. 73 tests green.
- **2026-07-18** — Story 4.1: event contracts + RabbitMQ publisher (first async plumbing). `EventEnvelope<T>` + `WorkOrderCreated`/`WorkOrderScheduled` in Application; `RabbitMqConnection`/`RabbitMqEventPublisher` + direct exchange `artifice.events` in Infrastructure; correlation middleware in Api; handler publishes best-effort after commit. Verified end-to-end against a live broker. 72 tests green.
- **2026-07-18** — Story 3.4: integration coverage closed (Epic 3 complete). `ProductApiTests` + work-order additions (history-404, hold/advance rejections, full-lifecycle walk, concurrent-create smoke check); shared `ReadProblemCodeAsync`. 68 tests green. Flagged missing aggregate optimistic-concurrency (concurrent advance untested by design).
- **2026-07-18** — Story 3.3: RFC 7807 ProblemDetails contract with stable `code` per failure; boundary DTO validation; domain rejection codes flow up via `TransitionErrorCode`. Duplicate product create → 409. 59 tests green.
- **2026-07-18** — Story 3.2: cancellation. `Cancelled` terminal status + `cancel` endpoint; terminal guards generalised; Fault kept cancellable; stock released on cancel; latent Completed-can-be-held fix. 58 tests green.
- **2026-07-17** — Planning interview: vision locked (live self-hosted demo, React dashboard + live event feed, hybrid simulation, failure injection, pipeline-first). Renamed to ArtificeWorks; rewrote docs/Plan; created CLAUDE.md + this file.
